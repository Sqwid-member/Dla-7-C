// Пошук кнопки за ID
const button = document.getElementById('redirectButton');

// Додавання події кліку
button.addEventListener('click', () => {
    // Перенаправлення на інший сайт
    window.location.href = 'https://google.com'; // Замініть на потрібну URL
});
